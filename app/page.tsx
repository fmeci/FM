import Link from "next/link";
import { CareerMap } from "@/components/CareerMap";
import { Hero } from "@/components/Hero";
import { InterestConstellation } from "@/components/InterestConstellation";
import { SectionHeader } from "@/components/SectionHeader";
import { interests, nowItems, writing } from "@/data/site";
export default function HomePage() { return <><Hero /><section className="section split-section" aria-labelledby="about-preview"><div><SectionHeader id="about-preview" eyebrow="A bit about me" title="I like ambitious work, useful systems, and conversations that change how people think." /><div className="prose-block"><p>By day, I lead marketing teams and work with founders, operators, and specialists to grow brands, build systems, and make better decisions.</p><p>Outside of work, I am pulled toward chess, physics, AI, football, books, and the kind of questions that do not fit neatly into one category.</p></div><Link className="text-link" href="/about">More about me<span aria-hidden="true">+</span></Link></div><div className="principles-grid" aria-label="Things Fatjon cares about">{interests.slice(0, 6).map((item) => <article className="principle-card" key={item.title}><span className="mini-icon" aria-hidden="true">{item.symbol}</span><h3>{item.title}</h3><p>{item.short}</p></article>)}</div></section><section className="section band" aria-labelledby="career-preview"><SectionHeader id="career-preview" eyebrow="Work" title="From data systems to global growth." intro="My path has moved through information science, business intelligence, data science, marketing technology, brand, and leadership." /><CareerMap condensed /></section><section className="section constellation-section" aria-labelledby="interests-preview"><SectionHeader id="interests-preview" eyebrow="Interests" title="The personal operating system." intro="A few recurring obsessions: strategic games, first principles, beautiful teams, and ideas that reward patience." /><InterestConstellation /></section><section className="section dashboard-section" aria-labelledby="writing-preview"><div><SectionHeader id="writing-preview" eyebrow="Writing and notes" title="A place for the questions I keep returning to." /><div className="writing-list">{writing.slice(0, 3).map((post) => <Link className="writing-row" href="/writing" key={post.title}><span>{post.category}</span><strong>{post.title}</strong><small>{post.date}</small></Link>)}</div></div><aside className="now-panel" aria-label="What Fatjon is focused on now"><h2>Now</h2><ul>{nowItems.map((item) => <li key={item}>{item}</li>)}</ul></aside></section></>; }
